{
  "name": "ideographe",
  "version": "1.0.0",
  "description": "ideographe.fr website",
  "main": "index.js",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "check": "pre-commit run --all-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "gilles coissac",
  "license": "GPL-3.0",
  "dependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
